<nav>
	<li><%= link_to 'islands you have visited', user_path(@user)  %></li>
</nav>

<nav>
	<li><%= link_to 'remove this island from your list', user_island_path(@user, @island), method: :delete %></li>
</nav>

<h1> <%= @user.name %> </h1>

<h2><%= @island[:name].upcase %></h2>

<h3>Size_ <%= @island[:size] %> km2</h3>

<h3>Region_ <%= @island[:region].capitalize %> Ocean </h3>

<h3>Population_ <%= @island[:population] %> </h3>

<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <title>Simple markers</title>
    <style>
      html, body, #map-canvas {
        height: 100%;
        margin: 0px;
        padding: 0px
      }
    </style>
    <script src="https://maps.googleapis.com/maps/api/js?v=3.exp&signed_in=true"></script>
    <script>
function initialize() {
  var myLatlng = new google.maps.LatLng(-25.363882,131.044922);
  var mapOptions = {
    zoom: 4,
    center: myLatlng
  }
  var map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);

  var marker = new google.maps.Marker({
      position: myLatlng,
      map: map,
      title: 'Hello World!'
  });
}

google.maps.event.addDomListener(window, 'load', initialize);

    </script>
  </head>
  <body>
    <div id="map-canvas"></div>
  </body>
</html>